%h1 Listing transactions

%table
  %tr
    %th Amount
    %th Concept
    %th Category
    %th Date
    %th
    %th
    %th

  - @transactions.each do |transaction|
    %tr
      %td= transaction.amount
      %td= transaction.concept
      %td= transaction.category
      %td= transaction.date
      -#%td= link_to 'Show', account_transaction_path(@account, @transaction)
      %td= link_to 'Edit', edit_account_transaction_path(@account, transaction)
      -#%td= link_to 'Destroy', account_transaction_path(@account, @transaction), method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Income', new_account_income_path(@account)
= link_to 'New Expence', new_account_expence_path(@account)
