= simple_form_for(@transaction) do |f|
  = f.error_notification

  .form-inputs
    = f.input :amount
    = f.input :concept
    = f.association :category
    = f.input :date
    = f.association :user

  .form-actions
    = f.button :submit
